#include "holberton.h"

/**
 * mul - multiply two integers
 * @a: integer to multiply against b
 * @b: integer to multiply against a
 *
 * Return: multiplication result
 */
int mul(int a, int b)
{
	return (a * b);
}
